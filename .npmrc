# CTFrbt — pnpm configuration
# Strict dependency isolation for security

# Prevent packages from accessing undeclared dependencies
strict-peer-dependencies=true

# Hoist nothing — strict isolation (prevents phantom dependencies)
hoist=false

# Prevent lifecycle scripts from untrusted packages
ignore-scripts=true
